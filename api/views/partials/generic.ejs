<script>
    const setupGeneric = () => {
        if (data.issuanceDetails.certificateType == "generic"){
            setValue('detail1', `${data.applicationAuthor.foreNames} ${data.applicationAuthor.lastName}`)
            setValue('descr1', 'Applicant Name')
            
            setValidity()
            removeElement('presentContainer')
            setValue('detail4', data.applicationAuthor.idNo)
            setValue('descr4', 'Applicant Identity No.')
            
            const regNo = data.applicationDetails.companyRegNo

            setValue('detail6', formatDate(data.applicationAuthor.dateOfBirth))
            setValue('descr6', 'Date of Birth')

            setValue('detail5', data.applicationAuthor.gender)
            setValue('descr5', 'Gender')

            setValue('detail7', data.applicationAuthor.idType)
            setValue('descr7', 'ID Type')

            setValue('detail8', data.applicationAuthor.nationality)
            setValue('descr8', 'Nationality')

            setValue('detail9', data.applicationAuthor.primaryPhoneNumber)
            setValue('descr9', 'Phone')

            showElement('segmented4')

            
            setValue('detail10', data.applicationAuthor.placeOfBirth)
            setValue('descr10', 'Place of Birth')

            setValue('detail11', data.applicationAuthor.primaryEmailAddress)
            setValue('descr11', 'Email')

            setValue('detail12', data.applicationAuthor.primaryPhysicalAddress)
            setValue('descr12', 'Address')

            removeElement('tableHeader')
            setValue('presentation','This licence is presented to: ')
        }
    }

    //function that add space to cammel case
    const addSpace = (str) => {
        return str.replace(/([A-Z])/g, ' $1').replace(/^./, function(str){ return str.toUpperCase(); })
    }

    //function that removes timezone from date
    const removeTimeZone = (date) => {
        if (date.includes('+')){
            return date.split('+')[0]
        }
        return date
    }
</script>